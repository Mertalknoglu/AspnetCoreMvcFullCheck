@* @using TalepYonetim.Models.ViewModel
@model List<RequestListViewModel>

@{
    ViewData["Title"] = "Talepler";
}

<div class="card">
    <h5 class="card-header">Talepler Listesi</h5>
    <div class="table-responsive text-nowrap">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th></th>
                    <th>Talep Sahibi</th>
                    <th>Talep Türü</th>
                    <th>Açıklama</th>
                    <th>Tarih</th>
                    <th>Durum</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var request in Model)
                {
                    <tr>
                        <td>@request.Id</td>
                        <td>@request.UserName @request.UserSurname</td>
                        <td>@request.RequestType</td>
                        <td>@request.Description</td>
                        <td>@request.Date.ToString("dd.MM.yyyy")</td>
                        <td>
                            <span class="badge bg-label-primary me-1">@request.RequestStatus</span>
                        </td>
                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="@Url.Action("Edit", "Request", new { id = request.Id })">
                                        <i class="bx bx-edit-alt me-1"></i> Düzenle
                                    </a>
                                    <a class="dropdown-item text-danger" href="@Url.Action("Delete", "Request", new { id = request.Id })"
                                       onclick="return confirm('Bu talebi silmek istediğinize emin misiniz?');">
                                        <i class="bx bx-trash me-1"></i> Sil
                                    </a>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
 *@
